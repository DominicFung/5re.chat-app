
import ApiKey from './components/apikey'
import PageOrLayout from './components/pageorlayout'

# Getting Started

  1. Login using GitHub ✅
  2. Add your Discord Server ID ✅
  3. Add `FireChat` to your NextJS App!

## Create a component called `Chat.tsx`

*This is your actual key! Please Copy as is!*

<ApiKey />

## Add `Chat.tsx` to your `app/page.tsx` or `app/layout.tsx`

You can add **5re.chat** to your `page.tsx` if you want the chat to only be available on a particular page. Adding to `layout.tsx` instead will make it available across multiple pages.

<PageOrLayout />